String Comparison Program (C#)
📌 Description

This C# console program demonstrates:

Concatenation of two strings to form a full name.

Comparison of two strings using String.Compare().

The program prints:

The full name after combining first and last names.

The result of string comparison:

0 if both strings are equal

< 0 if the first string is less than the second

> 0 if the first string is greater than the second

🧠 Features

String Concatenation
Combines F_name and L_name into Full_name using +.

String Comparison
Uses String.Compare(F_name, L_name) to compare the first and last names.

Console Output
Displays both the full name and the comparison result.
